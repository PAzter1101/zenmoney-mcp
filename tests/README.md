# Тесты для ZenMoney MCP Server

## 📊 Статистика
- **38 тестов** покрывают основной функционал
- **Покрытие кода: 49%** основных модулей
- **100% успешных прохождений** всех тестов

## 🏗️ Структура тестов

### Юнит-тесты (`unit/`)
- `test_basic_functionality.py` - базовый функционал и модели транзакций
- `test_models.py` - модели данных (Account, Category)
- `test_formatters.py` - утилиты форматирования вывода
- `test_validators.py` - валидаторы данных
- `test_transaction_model.py` - расширенные тесты модели транзакций
- `test_transaction_detail_tool.py` - инструмент детализации транзакций

### Интеграционные тесты (`integration/`)
- `test_mcp_server.py` - тесты MCP сервера и регистрации инструментов

### Вспомогательные файлы
- `fixtures.py` - общие фикстуры для тестов
- `conftest.py` - конфигурация pytest

## 🚀 Запуск тестов

### Базовые команды
```bash
# Все тесты
python -m pytest tests/

# Только юнит-тесты
python -m pytest tests/unit/

# Только интеграционные тесты
python -m pytest tests/integration/

# Подробный вывод
python -m pytest tests/ -v
```

### Покрытие кода
```bash
# С отчетом покрытия
python -m pytest tests/ --cov=src --cov=models --cov=utils --cov-report=term-missing

# HTML отчет покрытия
python -m pytest tests/ --cov=src --cov=models --cov=utils --cov-report=html
```

### Отладка
```bash
# Остановка на первой ошибке
python -m pytest tests/ -x

# Краткий вывод ошибок
python -m pytest tests/ --tb=short

# Тихий режим
python -m pytest tests/ -q
```

## 📈 Покрытие по модулям

| Модуль | Покрытие | Статус |
|--------|----------|--------|
| `models/account.py` | 100% | ✅ Отлично |
| `utils/formatters.py` | 95% | ✅ Отлично |
| `models/category.py` | 89% | ✅ Хорошо |
| `models/transaction.py` | 82% | ✅ Хорошо |
| `utils/validators.py` | 48% | ⚠️ Базовое |
| `src/client.py` | 42% | ⚠️ Базовое |
| `src/server.py` | 0% | ❌ Не покрыт |

## 🧪 Принципы тестирования

### Архитектура
- **Декомпозиция**: каждый тест-файл фокусируется на одном компоненте
- **Фикстуры**: переиспользование тестовых данных через fixtures.py
- **Изоляция**: тесты не зависят друг от друга

### Покрытие
- **Позитивные сценарии**: основной функционал работает корректно
- **Граничные случаи**: обработка пустых данных, None значений
- **Валидация**: проверка входных параметров и форматов

### Качество
- **Понятные названия**: каждый тест четко описывает что проверяется
- **Минимальный код**: только необходимая логика для проверки
- **Быстрое выполнение**: все тесты выполняются за < 1 секунды
