# ZenMoney MCP Server

Model Context Protocol (MCP) сервер для работы с API ДзенМани.

## 🎯 Функционал

### 🔐 Аутентификация
- `set_token` - Установка Bearer токена для доступа к API

### 📊 Получение данных
- `get_transactions` - Получение транзакций с фильтрацией по периодам
- `get_categories` - Список всех категорий пользователя
- `get_accounts` - Информация о счетах пользователя
- `get_merchants` - Список торговцев/получателей

### 🔍 Анализ данных
- `analyze_period` - Анализ транзакций за период (статистика, категоризация)
- `find_uncategorized` - Поиск некатегоризированных транзакций
- `analyze_patterns` - Анализ паттернов категоризации по торговцам
- `detect_duplicates` - Поиск возможных дублей транзакций

### 🎯 Категоризация
- `suggest_categories` - Предложения категорий для транзакций
- `apply_rules` - Применение правил категоризации
- `create_rules` - Создание правил на основе исторических данных

### 📈 Отчеты
- `spending_report` - Отчет по тратам за период
- `category_breakdown` - Разбивка по категориям
- `merchant_analysis` - Анализ трат по торговцам
- `budget_tracking` - Отслеживание бюджета

## 🏗️ Архитектура

```
mcp_server/
├── src/
│   ├── server.py          # Основной MCP сервер
│   └── client.py          # API клиент ДзенМани
├── tools/
│   ├── auth.py           # Инструменты аутентификации
│   ├── data.py           # Инструменты получения данных
│   ├── analysis.py       # Инструменты анализа
│   └── reports.py        # Инструменты отчетов
├── models/
│   ├── transaction.py    # Модели транзакций
│   ├── category.py       # Модели категорий
│   └── account.py        # Модели счетов
├── utils/
│   ├── filters.py        # Утилиты фильтрации
│   ├── formatters.py     # Форматирование вывода
│   └── validators.py     # Валидация данных
└── requirements.txt      # Зависимости MCP
```

## 🚀 Использование

### Установка
```bash
pip install -r mcp_server/requirements.txt
```

### Запуск сервера
```bash
python mcp_server/src/server.py
```

### Подключение к MCP клиенту
```json
{
  "mcpServers": {
    "zenmoney": {
      "command": "python",
      "args": ["path/to/zenmoney-mcp/src/server.py", "--token", "YOUR_ZENMONEY_TOKEN"]
    }
  }
}
```

Или через переменную окружения:
```json
{
  "mcpServers": {
    "zenmoney": {
      "command": "python",
      "args": ["path/to/zenmoney-mcp/src/server.py"],
      "env": {
        "ZENMONEY_TOKEN": "YOUR_ZENMONEY_TOKEN"
      }
    }
  }
}
```

## 📝 Примеры использования

### Базовый анализ
```
1. set_token - установить токен
2. analyze_period year=2025 month=1 - анализ января 2025
3. find_uncategorized year=2025 - найти проблемные транзакции
```

### Создание правил
```
1. analyze_patterns year=2024 - анализ паттернов 2024
2. create_rules min_confidence=3 - создать правила
3. apply_rules - применить к 2025 году
```

### Отчеты
```
1. spending_report year=2025 month=1 - отчет по тратам
2. category_breakdown period=2025-01 - разбивка по категориям
3. merchant_analysis top=10 - топ торговцы
```

## 🔧 Конфигурация

Сервер использует переменные окружения или конфигурационные файлы:
- `ZENMONEY_TOKEN` - Bearer токен
- `ZENMONEY_BASE_URL` - URL API (по умолчанию: https://api.zenmoney.ru)

## 📚 API Reference

Подробная документация по каждому инструменту доступна через MCP протокол после подключения к серверу.
